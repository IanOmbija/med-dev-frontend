import { MostVisitsDirective } from './most-visits.directive';

describe('MostVisitsDirective', () => {
  it('should create an instance', () => {
    const directive = new MostVisitsDirective();
    expect(directive).toBeTruthy();
  });
});
